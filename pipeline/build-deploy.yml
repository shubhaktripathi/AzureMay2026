parameters:
- name: SF_CLIENT_ID
  type: string 
- name: SF_USERNAME
  type: string
- name: SF_LOGIN_URL
  type: string
- name: SECURE_FILE_NAME
  type: string




stages:
  - stage: build
    jobs:
      - job: buildJob
        steps: 
          -script: echo Reusable JOb